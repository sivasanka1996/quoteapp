rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Test mode — open access until auth is added before handover
      allow read, write: if true;
    }
  }
}
